# api-gateway

Çalıştırma (lokal):
- mvn clean package
- java -jar target/api-gateway-0.0.1-SNAPSHOT.jar

Docker (compose ile): docker-compose dosyanıza gateway servisini ekleyin ve JWT_SECRET çevresel değişkenini set edin.

